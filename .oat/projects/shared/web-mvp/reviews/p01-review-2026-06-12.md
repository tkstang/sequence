---
oat_generated: true
oat_generated_at: 2026-06-12
oat_review_scope: p01
oat_review_type: code
oat_project: .oat/projects/shared/web-mvp
oat_review_verdict: pass
---

# Code Review: p01 (Foundation & Salvage)

**Reviewed:** 2026-06-12
**Scope:** Phase 1 — workspace scaffold, tooling gates, package skeletons, board map salvage, card assets, legacy deletion, git hooks/worktree scripts (p01-t01 … p01-t11)
**Files reviewed:** 179 changed (2,351 insertions / 19,458 deletions)
**Commits:** 11 (`69b8fa0`..`3849ad5`)

## Summary

Phase 1 is clean and complete. All eleven tasks landed as planned, all four root gates (`typecheck`, `lint`, `format:check`, `test`) are green on a clean working tree, the web skeleton builds, the board map is salvaged verbatim and correct, card assets are complete with proper LGPL attribution, all legacy code (including the committed GCP service key) is deleted with no firebase references remaining outside `.oat/`, and the git hooks function as specified. The five recorded deviations are all well-reasoned tooling adaptations that do not hide any real problem. Verdict: **pass**.

## Findings

### Critical

None

### Important

None

### Minor

- **Heavy J/Q/K card SVGs exceed the ~50% reduction target** (`apps/web/public/cards/JC.svg` = 157 KiB; QC/QS/KH/KS in the 76–81 KiB range)
  - Issue: Plan p01-t09 set a soft target of <50% per-file reduction and the design (line 309) flags face cards at 100–230 KB as the real perf work. Several face cards remain heavy after SVGO.
  - Suggestion: No action in Phase 1 — this is explicitly an accepted advisory (implementation.md Phase Summary) with a documented WebP-rasterization contingency deferred to p06 (design Risks line 508–509, p06 task list "SVG preloading"). Track it at p06, not here.

- **manage-hooks.js emits a `MODULE_TYPELESS_PACKAGE_JSON` warning** (`tools/git-hooks/manage-hooks.js`)
  - Issue: `pnpm hooks:setup` prints a Node ESM reparse warning because root `package.json` lacks `"type": "module"` while the script uses ESM syntax. Functionally harmless (hooks install correctly, idempotent).
  - Suggestion: Optional — either add `"type": "module"` to root `package.json` or rename the script `.mjs`. Already noted as an advisory in implementation.md. Not a Phase 1 blocker.

- **`@sequence/api` listed as a runtime dependency of `apps/web`** (`apps/web/package.json`)
  - Issue: Design intends the web app to consume `@sequence/api` as a **type-only** import of `AppRouter` (p05-t02). It is declared under `dependencies` rather than `devDependencies`.
  - Suggestion: Cosmetic for a skeleton; type-only imports still need the package resolvable and tree-shaking drops it from the runtime bundle. Revisit if/when p05-t02 wires the client. No action required now.

## Requirements/Design Alignment

**Evidence sources used:** `plan.md` (Phase 1 task specs), `design.md` (architecture, monorepo layout, tooling, SVG/perf risks, phase plan), `implementation.md` (Phase Summary, Deviations table, Test Results), `spec.md` (referenced), legacy source at `legacy-final` git tag (board map verbatim comparison).

### Phase Exit Criteria Coverage

| Exit criterion | Status | Notes |
| --- | --- | --- |
| Empty packages build | implemented | `pnpm --filter @sequence/web build` succeeds (Next 16, 3 static routes); game-logic/api are TS-source packages, typecheck green |
| `pnpm typecheck` green (clean tree) | implemented | tsgo per-package, all 3 projects "Done", exit 0 |
| `pnpm lint` green | implemented | exit 0 (one non-blocking unicorn warning in optimize-cards.mjs) |
| `pnpm format:check` green | implemented | exit 0, 21 files correctly formatted |
| `pnpm test` green | implemented | 7 tests pass (6 board-map + 1 smoke), empty-tree guard works |
| Clean working tree | implemented | `git status` clean before and after build; `.next/`, `dist`, `node_modules`, `.env*` gitignored |
| Legacy fully deleted | implemented | all of `pages/ components/ db/ services/ utils/ app/ styled-system/`, panda/eslint/prettier configs, service key, repomix/rewrite docs, root tsconfig.json gone |
| No firebase refs outside `.oat/` | implemented | `git grep -l firebase -- ':!.oat'` returns nothing |
| Board map correct | implemented | 100 cells, exactly 4 corners (one per corner), 48 distinct codes ×2, no jacks; matches `legacy-final:utils/game.js` verbatim |
| Card assets complete | implemented | 52 faces (13 ranks × 4 suits) + 2 backs + `ATTRIBUTION.md` (LGPL-3, Chris Aguilar, source/copyright); `10*.svg` aliases dropped, `T*` naming matches board codes |
| Git hooks functional | implemented | commit-msg accepts valid `pNN-tNN`, rejects malformed + bad scope shape, allows non-phase scopes, skips merges; pre-commit scopes oxlint/oxfmt to staged source; `hooks:setup` installs symlinks idempotently; `worktree:validate` passes |

### Per-Task Verification

All 11 tasks verified against their plan-declared verify commands; every commit's intent is satisfied by the shipped code. tsconfig.base is strict (ES2022, bundler, isolatedModules, noUncheckedIndexedAccess); oxlint config carries react/react-hooks/jsx-a11y/import/nextjs plugins; vitest workspace globs `packages/*`+`apps/*`; package names (`@sequence/game-logic`, `@sequence/api`, `@sequence/web`) and `game-logic` framework-free dependency boundary (zod only) all match design.

### Deviations Review (5 recorded — all accepted, none hide a problem)

1. **lint/format scoped to whole tree (not `pnpm -r`)** — correct: oxlint/oxfmt are single-binary whole-tree tools driven by root config; per-package fan-out would mis-scope and duplicate config. Confirmed in plan p01-t03 itself.
2. **vitest glob `apps/*` + run-tests.mjs guard** — sound workaround for vitest erroring on a literal not-yet-existing path; guard correctly tolerates the empty-tree case. `apps/web` is the only app, so equivalent.
3. **`allowImportingTsExtensions` added to tsconfig.base** — required for explicit `.ts` import specifiers under bundler + noEmit; foundational and correct.
4. **legacy root `tsconfig.json` also deleted** — justified: Next/Panda-era remnant scanning deleted dirs; no package extends it (all extend `tsconfig.base.json`). Reinforces "legacy gone."
5. **validate.sh adapted (not kept as-is)** — resolves an internal plan conflict (the as-is script referenced stoa-only `type-check`/`documentation`); the adaptation is the only way to satisfy the t11 "validate.sh passes" verify.

### Extra Work (not in declared requirements)

None of significance. The `scripts/run-tests.mjs` guard and `apps/*` glob are deviation-justified mechanisms, not scope creep.

### Outstanding operator/orchestrator follow-ups (carry forward, not Phase 1 defects)

- **Operator must revoke the GCP service key in Google Cloud** — deletion from the tree does not remove it from git history; revocation is the real mitigation (plan p01-t10, implementation.md).
- **`scripts/worktree/init.sh` is stoa-specific** — references `scripts/sync-archived-projects-from-s3.sh` and `oat local sync`; left as-is per plan but must be reviewed/adapted before the p02/p03 worktree bootstrap.

## Verification Commands

Run these to reproduce the review's findings:

```bash
# Root gates (all must exit 0)
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test

# Empty-package build
pnpm --filter @sequence/web build

# Legacy scrub
git grep -l "firebase" -- ':!.oat'    # expect: no output

# Board map vs legacy (expect identical rows)
git show legacy-final:utils/game.js | grep -A11 "const boardMap"

# Card assets (expect 52 faces + 2 backs + ATTRIBUTION.md)
ls apps/web/public/cards/*.svg | grep -vc BACK   # 52
ls apps/web/public/cards/*BACK*.svg              # 2

# Git hooks
printf 'feat(p1-t1): x' > /tmp/bad && sh tools/git-hooks/commit-msg /tmp/bad   # rejected
pnpm hooks:setup
bash scripts/worktree/validate.sh
```

## Recommended Next Step

Run the `oat-project-review-receive` skill to record this review. No findings require plan tasks — the three Minor items are either accepted advisories deferred to p06 (heavy SVGs) or optional cosmetics (ESM warning, dep classification). Phase 1 is ready to merge and proceed to the p02 ∥ p03 parallel group.
