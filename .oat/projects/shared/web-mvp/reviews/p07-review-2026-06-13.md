---
oat_generated: true
oat_generated_at: 2026-06-13
oat_review_scope: p07
oat_review_type: code
oat_project: .oat/projects/shared/web-mvp
---

# Code Review: p07

**Reviewed:** 2026-06-13
**Scope:** p07 tasks p07-t01 through p07-t05 (`5e19158..HEAD`, 14 p07 commits)
**Files reviewed:** 30 (18 changed files plus p07 artifacts/context)
**Commits:** `3329bf2`, `375c365`, `6e3ae83`, `3896a8e`, `2664128`, `9bbcf76`, `57227bc`, `977b375`, `13fba71`, `b36afa6`, `953139f`, `6dfa56a`, `694b40e`, `2e10f64`

## Findings

### Critical

None

### Important

- **NFR2 production latency is not met and should block final completion** (`.oat/projects/shared/web-mvp/handoff.md:99`)
  - Issue: NFR2 is a P1 requirement with an acceptance target of move processing and broadcast under about 500ms (`.oat/projects/shared/web-mvp/spec.md:194`, `.oat/projects/shared/web-mvp/spec.md:198`, `.oat/projects/shared/web-mvp/spec.md:199`), and p07-t04 requires the latency spot-check to pass as part of production smoke (`.oat/projects/shared/web-mvp/plan.md:844`, `.oat/projects/shared/web-mvp/plan.md:846`). The handoff records functional realtime success but measured production values of `2548ms` to `3846ms` and explicitly says NFR2 is not passed. Treating this as only an operator follow-up would close the project while an in-scope P1 acceptance criterion is known to fail.
  - Fix: Do not mark p07/final complete until NFR2 is either fixed and re-verified in production, or the spec/plan/design are explicitly revised to accept a deferred P1 performance gap. The likely fix path is to profile and reduce authenticated `game.makeMove` / broadcast latency, including Better Auth session lookup, `gamePlayerProcedure`, game-state load, per-seat hand/event writes, and API/DB region/co-location. Re-run production latency probes after the fix and record measured values under normal conditions.
  - Requirement: NFR2

### Medium

None

### Minor

- **Physical-phone mobile smoke remains unperformed** (`.oat/projects/shared/web-mvp/handoff.md:97`)
  - Issue: p07-t04 specifically called for a 375px mobile pass on a real phone (`.oat/projects/shared/web-mvp/plan.md:844`). The handoff honestly records that the pass was automated in a 375px / Pixel 5 viewport and that no real physical phone pass was performed (`.oat/projects/shared/web-mvp/handoff.md:100`, `.oat/projects/shared/web-mvp/handoff.md:149`). This does not by itself invalidate NFR3, whose acceptance criterion is a 375px-wide playable viewport (`.oat/projects/shared/web-mvp/spec.md:201`, `.oat/projects/shared/web-mvp/spec.md:205`), but it is a residual device/browser risk before broader playtesting.
  - Suggestion: Run one physical mobile browser smoke before broader operator playtesting and append the result to `handoff.md`. Keep this as a verification caveat unless the team decides the p07-t04 "real phone" step is mandatory for final signoff.

## Disposition

**p07 disposition:** Fail.

The deploy configuration, production cookie/CORS/proxy/rate-limit hardening, Railway/Vercel handoff evidence, and local gates are otherwise in good shape. NFR2 should block final project completion rather than remain only an operator follow-up, because the current artifact set records a known miss against a P1 requirement in the final deploy phase.

## Summary

p07 completed the deployment surface: Railway Dockerfile/config, predeploy migration wiring, Vercel/Railway env documentation, cross-site cookie behavior, `TRUST_PROXY=false` hardening, forged-XFF limiter mitigation, and operator handoff notes are implemented and documented. The blocking gap is production performance: the recorded `game.makeMove`/broadcast probes are far above the NFR2 target, so p07 should not be marked passed until the requirement is fixed or formally descoped.

## Requirements/Design Alignment

**Evidence sources used:** `.oat/projects/shared/web-mvp/plan.md`, `.oat/projects/shared/web-mvp/spec.md`, `.oat/projects/shared/web-mvp/design.md`, `.oat/projects/shared/web-mvp/discovery.md`, `.oat/projects/shared/web-mvp/implementation.md`, `.oat/projects/shared/web-mvp/state.md`, `.oat/projects/shared/web-mvp/handoff.md`, changed files in `5e19158..HEAD`, and targeted live endpoint checks.

### Requirements Coverage

| Requirement / Task | Status | Notes |
| --- | --- | --- |
| p07-t01 API Dockerfile + Railway config | implemented | `packages/api/Dockerfile` is workspace-aware and starts `@sequence/api`; `railway.json` uses the Dockerfile, predeploys `drizzle-kit migrate`, sets `/health`, disables sleep, and configures restart policy. |
| p07-t02 Railway deploy | implemented | `handoff.md` records Railway project/service/environment, public API URL, deployment IDs, migration success, healthcheck, and WS upgrade. Reviewer rechecked public `/health` successfully. |
| p07-t03 Vercel deploy | implemented | `handoff.md` records Vercel project/env/build settings and current alias. Reviewer rechecked the Vercel alias and non-mutating CORS preflight to the API. |
| p07-t04 Production smoke + checks | partial | Functional smoke, guest/auth cookie round-trips, forged-XFF limiter hardening, tier audit, and automated 375px pass are recorded. NFR2 latency is explicitly not met; real physical phone pass is not done. |
| p07-t05 Operator handoff notes | implemented | `handoff.md` includes URLs, env checklist, functional FR test script, NFR statuses, known limitations, and operator follow-ups with no secrets observed. |
| NFR2 Real-time responsiveness | missing | Production probes are `2548ms` to `3846ms`, above the about 500ms acceptance target. |
| NFR3 Responsive design | implemented with caveat | Automated 375px viewport coverage passed; physical phone pass remains a residual risk. |
| NFR5 Security hygiene | implemented | No secrets observed in `handoff.md`; production cookies default to `SameSite=None; Secure`; CORS is credentialed to the configured web origin; anonymous invite throttling is hardened against forged XFF. |
| NFR6 Hobby-tier cost envelope | implemented | Handoff records one Vercel Hobby project, one Railway API service, Neon Postgres, and no paid-tier-only dependency. |

### Extra Work (not in declared requirements)

No significant scope creep. The Vercel git-hook setup fix and the GameOver concede-result preservation were deploy/smoke-driven fixes needed to make the p07 production path succeed.

## Code Quality Notes

- `railway.json` and `packages/api/Dockerfile` align with the design's Railway API deploy strategy: Dockerfile build, predeploy migration, `/health`, and no boot-time migration.
- Production cookies are centralized in `packages/api/src/user/cookie-attributes.ts`, shared by Better Auth sessions and guest cookies, and covered by tests.
- CORS remains credentialed and single-origin via `env.WEB_ORIGIN`; live preflight returned `access-control-allow-origin: https://sequence-cyan.vercel.app` and credentials for the production origin.
- Proxy/rate-limit behavior is defensible for the observed Railway behavior: `TRUST_PROXY` defaults to false, and anonymous invite preview/join share a process-local limiter bucket to prevent forged-XFF bypass. The tradeoff is documented in the handoff.

## Verification Commands

Commands run during review:

```bash
git branch --show-current
git status --short
git diff --name-only 5e19158..HEAD
git log --oneline --no-merges 5e19158..HEAD
curl -fsS -m 10 https://sequence-api-production-8687.up.railway.app/health
curl -I -fsS -m 10 https://sequence-cyan.vercel.app/
curl -i -X OPTIONS -m 10 -H 'Origin: https://sequence-cyan.vercel.app' -H 'Access-Control-Request-Method: POST' https://sequence-api-production-8687.up.railway.app/trpc/health.me
curl -i -X OPTIONS -m 10 -H 'Origin: https://evil.example' -H 'Access-Control-Request-Method: POST' https://sequence-api-production-8687.up.railway.app/trpc/health.me
pnpm --filter @sequence/api exec vitest run src/env.test.ts src/user/cookie-attributes.test.ts src/server-config.test.ts src/shared/rate-limit-middleware.test.ts src/game/routes/join-game.test.ts
pnpm typecheck
pnpm lint
pnpm format:check
pnpm test
```

Observed results:

- Branch was `2026`; worktree was clean before artifact creation.
- Focused p07 API tests passed: 5 files, 33 tests.
- Root typecheck passed.
- Root lint passed with warnings only.
- Root format check passed.
- Root test passed: 58 files, 385 tests.
- Live API `/health` returned `{"status":"ok"}`.
- Vercel alias returned HTTP 200.
- Production CORS preflight returned the configured web origin with credentials.

Verification not rerun during review:

- Full mutating production signup/login, guest join, pass-and-play, two-browser realtime, and latency probes were not rerun by the reviewer to avoid creating additional production data. Review relied on `handoff.md` for those mutating smoke records and rechecked only non-mutating live endpoints.
- Docker build/container `/health`, Railway deploy logs, Railway predeploy migration logs, and Vercel deploy logs were not rerun; review checked their recorded handoff evidence and current config.

## Recommended Next Step

Run the `oat-project-review-receive` skill to convert findings into plan tasks. The NFR2 finding should be converted into a blocking final-phase fix task unless the project owner explicitly accepts a spec/plan change that defers NFR2.
