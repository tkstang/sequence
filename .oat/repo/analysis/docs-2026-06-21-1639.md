---
oat_generated: true
oat_generated_at: 2026-06-21
oat_analysis_type: docs
oat_analysis_mode: full
oat_docs_target: docs
oat_analysis_commit: bdef3ed93692960159f173479206873ae5070041
---

# Docs Analysis: sequence (Sequence Online)

**Date:** 2026-06-21
**Mode:** full
**Docs Target:** `docs/`
**Surface Type:** docs-tree
**Commit:** bdef3ed

## Summary

- **Files evaluated:** 8 closely evaluated (`docs/architecture.md`, `docs/development.md`, `docs/deployment.md`, `README.md`, `AGENTS.md`, `apps/web/README.md`, `packages/api/README.md`, `packages/game-logic/README.md`); `bruno/README.md` and `tools/git-hooks/README.md` referenced.
- **Directories assessed:** 1 docs directory (`docs/`), plus root-level Markdown.
- **Index coverage:** 0% of docs directories contain `index.md` (`docs/` has no `index.md`).
- **Findings:** 0 Critical, 4 High, 8 Medium, 5 Low.
- **Delta scope:** N/A (full mode — no prior docs tracking commit stored).
- **Evidence-backed recommendations:** 13.
- **Open questions / ask-user items:** 2 (migrate `docs/` to an OAT/Fumadocs docs app vs. keep flat docs-tree; add GitHub OAuth vars to `.env.example` for parity).
- **Contradicted claims:** 0 (34 repo-checkable claims verified, 0 contradicted).
- **Coverage gaps / content opportunities:** 8.
- **Generated index findings:** 0 (no generator; flat docs-tree).
- **Broken or extensionless local docs links:** 0.
- **Markdown hygiene findings:** 0 material defects.
- **Local guidance gaps:** 3 (no docs-authoring contract in `AGENTS.md`; no `index.md`/`## Contents` convention; no analyze/apply routing).
- **Owner-review gaps:** 2 (external Vercel project settings; production URL liveness).

**Headline:** The existing docs are unusually accurate and well-written — every repo-checkable substantive claim verified against source. The gaps are *structural and coverage-shaped*, not correctness-shaped: there is no `docs/index.md` entrypoint, no game-rules/how-to-play page in the published docs (a canonical ruleset exists but is parked in `.oat/repo/reference/planning/` — relocate it; see High-2 / Rec 2), and no signature-level reference for the three things the codebase treats as contracts (the `@sequence/game-logic` engine, the tRPC API, and the DB schema). Cross-cutting setup/env content is duplicated across five files with drift risk.

## Docs Inventory

| #   | Type      | Path                              | Status | Notes                                                                 |
| --- | --------- | -------------------------------- | ------ | -------------------------------------------------------------------- |
| 1   | directory | `docs/`                          | issues | No `index.md` entrypoint / `## Contents` map (top-level docs section) |
| 2   | page      | `docs/architecture.md`           | pass   | Accurate; system boundaries, flow, auth, realtime, scaling, security |
| 3   | page      | `docs/development.md`            | pass   | Accurate; install, env, run, tests, Playwright, DB, UI, Bruno        |
| 4   | page      | `docs/deployment.md`            | pass   | Accurate; Railway/Vercel/Neon, env vars, smoke checks, constraints   |
| 5   | root-md   | `README.md`                      | pass   | Strong overview; "More Documentation" list partial (see Low-2)       |
| 6   | root-md   | `AGENTS.md`                      | pass   | Project guidance; lacks docs-authoring contract (see Local Guidance) |
| 7   | pkg-md    | `apps/web/README.md`            | pass   | Accurate; full route table; overlaps `docs/development.md` (dup)     |
| 8   | pkg-md    | `packages/api/README.md`        | pass   | Accurate overview; only "API reference" surface but not enumerated   |
| 9   | pkg-md    | `packages/game-logic/README.md` | pass   | Accurate; lists ~12 of ~40 exports, no signatures                     |
| 10  | pkg-md    | `bruno/README.md`               | pass   | Smoke-collection guide (referenced, complements docs)                |
| 11  | pkg-md    | `tools/git-hooks/README.md`     | pass   | Only home of commit-convention + hooks detail (referenced)           |

Excluded from the docs surface (tooling/hidden dirs): `.agents/`, `.claude/`, `.cursor/`, `.oat/`, `node_modules/`, `apps/web/public/cards/ATTRIBUTION.md` (asset attribution).

## Findings

### Critical

None. The docs contain no destructive, unsafe, or auth/security-misleading guidance. (Production-safety notes — `TRUST_PROXY=false`, cross-site cookie modes, "do not point `DATABASE_URL` at production" — are present and correct.)

### High

1. **`docs/` has no `index.md` entrypoint or `## Contents` map**
   - File: `docs/` (directory)
   - Issue: The top-level docs section has no `index.md`. There is no in-tree map of what the docs contain; discovery depends entirely on the root `README.md` "More Documentation" list (`README.md:130-137`), which is itself incomplete (see Low-2). Per the directory-assessment rubric, a top-level docs section with no `index.md` is a High gap.
   - Evidence: `find docs -type f` returns only `architecture.md`, `development.md`, `deployment.md`; no `index.md`. `README.md:130-137` is the only aggregation point.
   - Confidence: high
   - Disclosure: inline
   - Link Target: N/A
   - Fix: Add `docs/index.md` with a `## Contents` section mapping the three existing pages (and any new pages from the Content Opportunities below), grouped by intent (Getting started / Concepts / Reference / Operations).

2. **No canonical Sequence game-rules / how-to-play page in the published docs**
   - File: (missing in `docs/`) — suggest `docs/game-rules.md`
   - Issue: A canonical, human-readable ruleset **does exist** — `.oat/repo/reference/planning/2026-rewrite/game-instructions.md` is the full official Sequence ruleset (objective, corners, jacks, dead cards, dealing table, win conditions). But it lives in an internal planning/reference directory **outside the published `docs/` surface**, so players and new contributors won't find it, and it is the *full physical-game* ruleset that overshoots the MVP's implemented subset (it lists 2–12 players and physical-only rules like table-talk forfeits and loss-of-card, while the engine implements only `PlayerCount` 2/3/4/6 and auto-enforces draws/turns). The implemented behavior is otherwise encoded only in `@sequence/game-logic` source. So the gap is: no docs page, plus a source that must be reconciled against what actually ships.
   - Evidence: canonical prose ruleset `.oat/repo/reference/planning/2026-rewrite/game-instructions.md:7-98` (object/preparation/jacks/dead-card/players/equipment); implemented behavior in `packages/game-logic/src/types.ts:203-218` (`RuleViolation` 13 codes — a 14th case is enforced structurally rather than as a code; `GameMode` tap/drag, `PlayerCount` 2/3/4/6, `Team`), `packages/game-logic/src/jack-rules.ts` (one-/two-eyed jacks), `packages/game-logic/src/dead-cards.ts`, `packages/game-logic/src/sequence-detection.ts`, `packages/game-logic/src/win-conditions.ts` (`sequencesToWin`), `packages/game-logic/src/board-map.ts` (`isCorner`, wild corners), `packages/game-logic/src/create-game.ts` (deal counts).
   - Confidence: high
   - Disclosure: inline
   - Link Target: N/A
   - Fix: **Relocate** the existing ruleset from `.oat/repo/reference/planning/2026-rewrite/game-instructions.md` into the published docs as `docs/game-rules.md` (use `git mv` to preserve history), then **reconcile it against the implemented surface** — scope to the supported player counts (2/3/4/6), confirm sequences-to-win and per-count deal counts against `win-conditions.ts`/`create-game.ts`, and either mark physical-only rules (table-talk, loss-of-card) as auto-enforced/not-applicable in the digital game or omit them. Add the digital-specific concepts the official rules don't cover: tap vs drag mode, the over-five-run choice (`PendingChoice`), and natural-over-jack consumption. This relocation + reconciliation is `oat-docs-apply`'s job, not this read-only analysis pass; apply must read both `game-instructions.md` and the cited `game-logic` source before writing, and must not copy the physical ruleset verbatim.

3. **No enumerated tRPC / API reference for the cross-client contract**
   - File: (missing) — suggest `docs/api-reference.md` (or expand `packages/api/README.md`)
   - Issue: `AppRouter` is explicitly the typed multi-client contract (re-exported at `packages/api/src/index.ts:9`, consumed by web as a type, and named as the contract for a "future client"). The only API description (`packages/api/README.md:49-59`) is a deliberately abbreviated prose overview, not a per-procedure reference. There is no enumerated list of procedures, inputs/outputs, auth requirements, the version-guard contract, or the `RuleViolation`→`BAD_REQUEST` error contract.
   - Evidence: `packages/api/src/app-router.ts:12-22` (3 routers); `packages/api/src/game/game.router.ts:40-56` (15 game procedures); `packages/api/src/history/history.router.ts:10-14` (3 procedures); per-route Zod inputs in `packages/api/src/game/routes/*`; version guard in `packages/api/src/game/move-engine.ts:115-161`.
   - Confidence: high
   - Disclosure: inline
   - Link Target: N/A
   - Fix: Add `docs/api-reference.md` enumerating every procedure with kind/auth, input/output, the `version` optimistic-concurrency contract, the `RuleViolation`→`BAD_REQUEST` mapping, the `onGameEvent` subscription (snapshot-first + resume cursor), the anonymous rate-limit bucket on `preview`/`join`, and the Better Auth `/api/auth/*` surface.

4. **No signature-level `@sequence/game-logic` library reference**
   - File: (missing) — suggest `docs/game-logic-reference.md` (or expand `packages/game-logic/README.md`)
   - Issue: The package README calls this "the same domain contract" shared by API, web, tests, and future clients, but documents only ~12 of ~40 exports as a "Common exports" bullet list (`packages/game-logic/README.md:27-37`) with no signatures, no `MoveResult` ok/error shape, no `RuleViolation` catalog, and no `GameEvent` variant list.
   - Evidence: `packages/game-logic/src/index.ts` (single barrel, ~40 exports); `packages/game-logic/src/types.ts` (full domain types); `packages/game-logic/src/apply-move.ts:1-13` (`applyMove`/`MoveResult` contract docblock). Source is heavily docstringed.
   - Confidence: high
   - Disclosure: inline
   - Link Target: N/A
   - Fix: Add `docs/game-logic-reference.md` with the full export inventory by group, signatures for `applyMove`/`resolveSequenceChoice`/`turnInDeadCard`, the `MoveResult` ok/error union, the `RuleViolation` code catalog, the `GameEvent` variants, and the `Rng` determinism contract.

### Medium

1. **No consolidated environment-variable reference; env content duplicated across 5 files**
   - File: `README.md:41-60`, `docs/development.md:16-33`, `docs/deployment.md:37-57`, `apps/web/README.md:37-42`, `packages/api/README.md:16-38`
   - Issue: Env-var names, defaults, and required/optional status are restated in five places with no single source of truth. The canonical schema (`packages/api/src/env.ts`) defines 14 vars with defaults, transforms, and validation; the docs paraphrase subsets of it. This is the highest drift-risk surface in the docs (the `.env.example` GitHub-var gap in Low-3 is an example of exactly this drift).
   - Evidence: `packages/api/src/env.ts` (Zod schema, all vars + defaults + `.min(32)` on `BETTER_AUTH_SECRET` + `TRUST_PROXY` transform); five doc locations above.
   - Confidence: high
   - Disclosure: inline (canonical table) + link_only (others point to it)
   - Link Target: `docs/development.md#environment-reference` (or a new `docs/configuration.md`)
   - Fix: Add one canonical env table (var, scope api/web/deploy, required?, default, validation rule, prod value) and have the other docs link to it instead of re-listing.

2. **No data model / DB schema reference**
   - File: (missing) — suggest `docs/data-model.md`
   - Issue: Persistence is central (optimistic-concurrency `version`, event log as resume cursor, hand-privacy boundary), but there is no schema reference. Only inline docstrings exist.
   - Evidence: `packages/api/src/db/schema/games.ts:83-141` (jsonb board/deck/played/sequences, `version`, status/end-reason enums, self-FK `rematch_of`), `packages/api/src/db/schema/game-players.ts:27-54` (private `hand` jsonb, user-vs-guest seat), `packages/api/src/db/schema/game-events.ts:23-41` (`seq` cursor), migrations `packages/api/drizzle/0000–0003*.sql`.
   - Confidence: high
   - Disclosure: inline
   - Link Target: N/A
   - Fix: Add `docs/data-model.md` covering the three game tables + Better Auth tables, jsonb shapes and the state-mapping round-trip (`packages/api/src/game/state-mapping.ts`), the `version` concurrency model, the event-log triple duty (log/resume/replay), guest vs user seats, and FK cascade/SET NULL behavior.

3. **No testing-strategy documentation (only commands)**
   - File: `docs/development.md:52-89` (commands only)
   - Issue: Docs list test commands and the Playwright config, but never explain the test *layers* (game-logic unit, API integration via DB harness, web component, Playwright e2e) or how `DATABASE_URL_TEST` gates integration + e2e.
   - Evidence: `scripts/run-tests.mjs`, `vitest.workspace.ts`, `packages/api/src/test/harness.ts`, `packages/api/src/.../full-game.e2e.test.ts`, `packages/api/src/.../db-lock.ts`, `apps/web/e2e/`, `apps/web/playwright.config.ts:43-58`.
   - Confidence: medium
   - Disclosure: inline (as an expansion of `docs/development.md`)
   - Link Target: N/A
   - Fix: Add a `docs/testing.md` (or "Testing strategy" section in `docs/development.md`) describing the layers, the integration harness + DB test branch, and the `DATABASE_URL_TEST` gating.

4. **No CONTRIBUTING guide; commit convention + gates only discoverable indirectly**
   - File: (missing) — suggest root `CONTRIBUTING.md`
   - Issue: The conventional-commit + `pNN-tNN` scope convention lives only in `tools/git-hooks/README.md`; the PR-ready gate sequence and the game-logic framework-free rule live only in `AGENTS.md`. There is no contributor entrypoint.
   - Evidence: `tools/git-hooks/README.md` (commit convention, hooks), `AGENTS.md:37-49` (gates, game-logic purity, backlog constraints), `.oxlintrc.json`, `.oxfmtrc.json`.
   - Confidence: medium
   - Disclosure: inline
   - Link Target: N/A
   - Fix: Add `CONTRIBUTING.md` (commit convention, gate sequence, game-logic purity rule, oxlint/oxfmt, hooks, "do not document `/dev` or symbolic board as existing") and link it from `README.md` and `docs/index.md`.

5. **Setup/run/test content duplicated across `docs/` and package READMEs (progressive disclosure)**
   - File: `docs/development.md` ⇄ `apps/web/README.md:29-52` ⇄ `packages/api/README.md:16-47` ⇄ `README.md:62-128`
   - Issue: Run/test/migration instructions are restated in package READMEs and root README as well as `docs/development.md`. Some duplication is acceptable for package-local quickstarts, but the cross-cutting detail (env, full test matrix, migrations) should be canonical in `docs/` with package READMEs keeping only package-specific notes + a link.
   - Evidence: overlapping command/env blocks at the refs above.
   - Confidence: medium
   - Disclosure: link_only
   - Link Target: `docs/development.md`
   - Fix: Designate `docs/development.md` (+ the new env reference) canonical; trim package READMEs to package-specific notes plus a "See `docs/development.md`" link.

6. **API README `health` description is imprecise (calls a public route "authenticated")**
   - File: `packages/api/README.md:57`
   - Issue: `health` is summarized as "authenticated session/user health helpers", but `health.ping` is a **public** procedure (`packages/api/src/app-router.ts:14`); only `health.me` is authed (`:16`).
   - Evidence: `packages/api/src/app-router.ts:13-17` (`ping: publicProcedure`, `me: authedProcedure`).
   - Confidence: high
   - Disclosure: inline
   - Link Target: N/A
   - Fix: Reword to "health helpers (`ping` public, `me` authenticated)", or fold into the new `docs/api-reference.md`.

7. **No docs-authoring contract / `index.md` convention in local guidance**
   - File: `AGENTS.md` (Sequence Project Guidance section)
   - Issue: `AGENTS.md` covers code conventions but says nothing about docs authoring — no `index.md`/`## Contents` requirement, no `.md` link convention, and no routing of audits to `oat-docs-analyze` / bulk edits to `oat-docs-apply`. As the docs surface grows (8 candidate pages below), the absence of a contract invites inconsistency.
   - Evidence: `AGENTS.md:28-49` (project guidance, no docs section); no docs-app `AGENTS.md` exists.
   - Confidence: medium
   - Disclosure: inline
   - Link Target: N/A
   - Fix: Add a short "Documentation" block to `AGENTS.md` (or a `docs/AGENTS.md`) stating the `index.md` + `## Contents` contract, `.md` relative links, and analyze/apply routing.

8. **Decide docs tooling: keep flat docs-tree vs. migrate to an OAT/Fumadocs docs app**
   - File: `docs/` (whole surface)
   - Issue: There is no `mkdocs.yml` and no docs app. The surface is a flat 3-file tree today, but this analysis proposes up to 8 new pages across Getting-started/Concepts/Reference/Operations groupings. At that size, an OAT docs app (navigable, generated index, link validation) becomes worth considering. This is a direction decision, not a defect.
   - Evidence: flat `docs/` tree; `docs` tool pack installed (`.oat/config.json:19`); `oat-docs-bootstrap` skill available.
   - Confidence: medium
   - Disclosure: ask_user
   - Link Target: N/A
   - Fix: Ask the user whether to (a) keep a flat docs-tree with a hand-maintained `docs/index.md`, or (b) bootstrap an OAT/Fumadocs docs app. Do not migrate without confirmation.

### Low

1. **Root README "Useful routes" omits `/` and `/ping`**
   - File: `README.md:73-81`
   - Issue: The quickstart route list omits the public entry `/` and the `/ping` check; `apps/web/README.md:17-27` already lists both. Minor — both are non-essential to the quickstart.
   - Evidence: `apps/web/src/app/page.tsx`, `apps/web/src/app/ping/page.tsx`; `apps/web/README.md:19,27`.
   - Confidence: high
   - Disclosure: inline
   - Link Target: N/A
   - Fix: Optionally add `/` and `/ping`, or leave as a deliberate quickstart subset.

2. **Root README "More Documentation" list is incomplete**
   - File: `README.md:130-137`
   - Issue: Lists the three `docs/` pages and three package READMEs but omits `bruno/README.md`, `tools/git-hooks/README.md`, and `AGENTS.md`. Once `docs/index.md` exists, this list should defer to it.
   - Evidence: `README.md:130-137`; existing `bruno/README.md`, `tools/git-hooks/README.md`, `AGENTS.md`.
   - Confidence: high
   - Disclosure: inline
   - Link Target: `docs/index.md`
   - Fix: Complete the list, or replace it with a single link to `docs/index.md`.

3. **`.env.example` lists only Google OAuth vars while code + `deployment.md` support GitHub too**
   - File: `.env.example:24-26`
   - Issue: `development.md:17` names `.env.example` the "source of truth for variable names", but it lists only `GOOGLE_CLIENT_ID/SECRET` (commented), while `packages/api/src/env.ts:53-56` + `packages/api/src/user/auth.ts:14-29` support GitHub too, and `deployment.md:51-55` + `packages/api/README.md:35-36` list both. `.env.example` is the outlier. (Note: `.env.example` is a config file, not a `docs/` file — flag for owner; `oat-docs-apply` should not edit it without scope confirmation.)
   - Evidence: `.env.example:24-26`, `packages/api/src/env.ts:53-56`, `packages/api/src/user/auth.ts:14-29`, `deployment.md:51-55`.
   - Confidence: high
   - Disclosure: ask_user
   - Link Target: N/A
   - Fix: Add commented `GITHUB_CLIENT_ID/SECRET` to `.env.example` for parity (owner decision; outside the `docs/` edit scope).

4. **`development.md` quotes a partial form of the API `dev` command**
   - File: `docs/development.md:21-26`
   - Issue: The doc's surrounding text says the dev script "uses `node --env-file=.env`"; the actual script is `node --experimental-transform-types --env-file=.env --watch src/server.ts` (`packages/api/package.json:14`). The load-bearing claim (the `.env` path is package-local) is correct; this is a paraphrase, not an error.
   - Evidence: `packages/api/package.json:14`; `docs/development.md:25-26`.
   - Confidence: medium
   - Disclosure: omit (acceptable paraphrase; fix only if touching the file)
   - Link Target: N/A
   - Fix: Optional — note the abbreviation or quote the full script.

5. **`architecture.md` UI component list is non-exhaustive (not a defect)**
   - File: `docs/architecture.md:96-106`
   - Issue: The "UI State Shape" list uses "including:" and omits `ActiveGameControls`, `SequenceChoice`, `toasts`, and `controllers/`. Because it is explicitly non-exhaustive, this is not a defect — recorded only so it is not mistaken for a complete component inventory.
   - Evidence: `apps/web/src/app/game/[id]/components/` (additional components present); `docs/architecture.md:98` ("including:").
   - Confidence: high
   - Disclosure: omit
   - Link Target: N/A
   - Fix: None required; revisit only if a component reference page is added.

## Directory Contract Gaps

| #   | Directory | Gap                                                | Evidence                                            | Disclosure | Link Target | Severity | Recommended Fix                                                  |
| --- | --------- | -------------------------------------------------- | --------------------------------------------------- | ---------- | ----------- | -------- | --------------------------------------------------------------- |
| 1   | `docs/`   | Missing `index.md` with `## Contents` map           | `find docs` → 3 pages, no index; `README.md:130-137`| inline     | N/A         | High     | Add `docs/index.md` with a grouped `## Contents` map           |

## Generated Index and Local Map Findings

No generated index or local-map findings identified. The surface is a flat `docs-tree` with no MkDocs config, no Fumadocs `source.config.*`, and no generated root index or `meta.json`. `apps/web/next.config.ts` is the web application's Next.js config, not a docs-app generator.

## Authored Links, Contents, and Markdown Hygiene

| #   | Category   | File Ref | Issue | Evidence | Severity | Recommended Fix |
| --- | ---------- | -------- | ----- | -------- | -------- | --------------- |
| —   | —          | —        | —     | —        | —        | —               |

No authored link, `## Contents`, or Markdown hygiene findings identified.

- **Links:** All local relative Markdown links resolve. `README.md:132-137` → `docs/architecture.md`, `docs/development.md`, `docs/deployment.md`, `apps/web/README.md`, `packages/api/README.md`, `packages/game-logic/README.md` all exist; `docs/development.md:131`/`README.md:127` → `bruno/README.md` exists. Path references in `deployment.md` (`packages/api/Dockerfile`, `railway.json`, `packages/api/drizzle`) exist. This is a `docs-tree` (not Fumadocs), so the `.md`-suffix / `subdir/index.md` link convention does not apply; existing links already use `.md`.
- **Code fences:** All fenced blocks carry a language id; the repo's docs uniformly use ```bash for shell, which is the de-facto local convention (no conflicting `sh` guidance exists), so no fence-drift finding.
- **Headings:** One `# H1` per page; no empty headings.
- **Frontmatter:** None of the docs use frontmatter; for a flat docs-tree this is acceptable, so no description-length/truncation findings.

## Local Docs-App Guidance

| #   | Guidance Area              | Source Ref  | Status  | Evidence                          | Severity | Recommended Fix                                            |
| --- | -------------------------- | ----------- | ------- | --------------------------------- | -------- | --------------------------------------------------------- |
| 1   | `index.md` / `## Contents` | `AGENTS.md` | missing | `AGENTS.md:28-49` (no docs block) | Medium   | State the `index.md` + `## Contents` contract for `docs/` |
| 2   | `.md` relative links       | `AGENTS.md` | missing | `AGENTS.md:28-49`                 | Low      | Document `.md` relative-link convention                    |
| 3   | Analyze/apply boundaries   | `AGENTS.md` | missing | `AGENTS.md:28-49`                 | Medium   | Route docs audits → `oat-docs-analyze`, bulk edits → `oat-docs-apply` |

(No docs-app `AGENTS.md`, contributing page, or authoring guide exists. No stale analyze/apply aliases found — there is simply no docs guidance yet.)

## Accuracy Verification

34 repo-checkable substantive claims were verified across the three `docs/` pages: **34 verified, 0 unverified, 0 contradicted.** Representative and nuanced rows below; full per-claim evidence captured during investigation.

| #   | Docs Claim                                                                 | Docs Ref                       | Canonical Source Ref                                                                 | Verdict   | Severity | Notes                                                                 |
| --- | ------------------------------------------------------------------------- | ------------------------------ | ----------------------------------------------------------------------------------- | --------- | -------- | -------------------------------------------------------------------- |
| 1   | Better Auth mounted at `/api/auth/*`; tRPC HTTP+WS at `/trpc`; `GET /health` | `architecture.md:32-34`        | `packages/api/src/server.ts:134-148, 170-174, 127`                                  | verified  | N/A      |                                                                      |
| 2   | Web imports `AppRouter` as a TYPE; no API runtime in browser              | `architecture.md:17-18`        | `apps/web/src/lib/trpc/client.ts:1`; `packages/api/src/index.ts:9`                  | verified  | N/A      | Package root exports only `type AppRouter` + a string const          |
| 3   | Split transport: HTTP batch (`credentials: include`) + lazy WS for subs    | `architecture.md:20-26`        | `apps/web/src/lib/trpc/client.ts:53-78, 44`                                         | verified  | N/A      | `splitLink` on `op.type === 'subscription'`; `lazy.enabled`          |
| 4   | All listed mutations exist + validate seat + client `version`             | `architecture.md:51,56-60`     | `packages/api/src/game/game.router.ts:40-56`; `move-engine.ts:115-161`             | verified  | N/A      | Every procedure present; version CONFLICT on mismatch                |
| 5   | Reconnect snapshot-first; resends last tracked event id; replay/snapshot   | `architecture.md:57,63-65`     | `packages/api/src/game/routes/on-game-event.ts:48-242`                              | verified  | N/A      | `REPLAY_WINDOW = 500`; gap-replay then snapshot                      |
| 6   | Guest tokens signed with `BETTER_AUTH_SECRET`; httpOnly; same cookie mode   | `architecture.md:72-74`        | `packages/api/src/user/guest-tokens.ts:48-63`; `join-game.ts:125-153`              | verified  | N/A      | HMAC-SHA256; cookie attrs shared with session cookies               |
| 7   | `TimerService` coordinates timers; rehydrated from DB on boot              | `architecture.md:89-92`        | `packages/api/src/game/TimerService.ts:45-181`; `server.ts:198`                     | verified  | N/A      |                                                                      |
| 8   | Private hands redacted by API response mapping                             | `architecture.md:130-132`      | `packages/api/src/shared/realtime/redaction.ts:54-79, 162-234`                      | verified  | N/A      | Snapshot serializes only recipient's own hand                       |
| 9   | `TRUST_PROXY` read, defaults false; anon invite shares one limiter bucket   | `architecture.md:125-128`      | `packages/api/src/env.ts:35-43`; `server.ts:234-236`; `game.router.ts:32-38`        | verified  | N/A      | Anon join/preview keyed by fixed `'anonymous:join-preview'` bucket  |
| 10  | `railway.json`: Dockerfile builder, predeploy migrate, start, `/health`, region | `deployment.md:30-35`     | `railway.json:4,16-18,23`                                                            | verified  | N/A      | All five fields match exactly                                       |
| 11  | All required Railway env vars consumed by code                             | `deployment.md:39-48`          | `packages/api/src/env.ts:11,17,21,23,25,27,35,47,48`; reads in `server.ts`/`auth.ts`| verified  | N/A      | Every var schema-validated and consumed                            |
| 12  | `BETTER_AUTH_SECRET` "at least 32 characters"                              | `deployment.md:41`             | `packages/api/src/env.ts:17-19` (`z.string().min(32, …)`)                           | verified  | N/A      | Enforced at boot (fails fast) — not merely advisory                 |
| 13  | Playwright loads root `.env`, gates on `DATABASE_URL_TEST`, 1 worker, 2 projects | `development.md:76-89`    | `apps/web/playwright.config.ts:7-10,24,28-58`                                        | verified  | N/A      | Desktop 1280x900 + Pixel 5 375x812; servers only when test DB set   |
| 14  | Backlog `bl-d319` (playground) and `bl-821f` (symbolic board) exist        | `development.md:115,120`       | `.oat/repo/reference/backlog/items/dev-ui-component-playground.md:2-3`; `…/explore-symbolic-sequence-board-rendering.md:2-3` | verified | N/A | IDs in frontmatter; topics match |
| 15  | API `dev` script uses `node --env-file=.env` (package-local `.env`)        | `development.md:25-26`         | `packages/api/package.json:14`                                                       | verified  | Low      | Full script has extra flags; doc paraphrases (see Low-4)            |
| 16  | Optional GitHub & Google OAuth, config-gated by env presence              | `architecture.md:69-70`; `deployment.md:51-55` | `packages/api/src/user/auth.ts:14-29`; `env.ts:53-56`               | verified  | Low      | Code supports all four; `.env.example` lists only Google (Low-3)    |

## Coverage Review by Surface

| #   | Surface     | Repo Evidence                                                                 | Docs Coverage State | Missing or Thin Areas                                                                 | Owner Review Needed |
| --- | ----------- | ---------------------------------------------------------------------------- | ------------------- | ------------------------------------------------------------------------------------ | ------------------- |
| 1   | App/service | `packages/api/src/server.ts`, `game/`, `user/`, `db/schema/`                  | adequate            | Data model reference thin (Medium-2); testing strategy thin (Medium-3)               | no                  |
| 2   | API         | `app-router.ts`, `game/game.router.ts` (15 procs), `history.router.ts` (3)    | thin                | Enumerated per-procedure reference, auth/version/error contracts (High-3)            | no                  |
| 3   | Library     | `packages/game-logic/src/index.ts` (~40 exports), `types.ts`, `apply-move.ts`  | thin                | Signature-level reference, `MoveResult`/`RuleViolation`/`GameEvent` (High-4)         | no                  |
| 4   | Game domain | `game-logic` rules modules (jacks, dead-cards, sequences, win)                | no coverage         | Canonical ruleset / how-to-play (High-2)                                             | no                  |
| 5   | Operations  | `railway.json`, `packages/api/Dockerfile`, `docs/deployment.md`               | adequate            | Troubleshooting/common failure modes (folded into recs); external Vercel settings   | yes (Vercel project settings + prod URL liveness are external, not repo-checkable) |

## Content Opportunities

| #   | Capability Area                | Coverage State | Codebase Evidence (file refs)                                                                                              | Suggested Docs Location                         | Severity | Subtopics To Cover                                                                                                   |
| --- | ------------------------------ | -------------- | ------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------- |
| 1   | Sequence game rules / how-to-play | source exists outside docs  | **Canonical prose source:** `.oat/repo/reference/planning/2026-rewrite/game-instructions.md:7-98`. **Implemented behavior:** `game-logic/src/types.ts`, `jack-rules.ts`, `dead-cards.ts`, `sequence-detection.ts`, `win-conditions.ts`, `board-map.ts`, `create-game.ts`  | Relocate (`git mv`) → `docs/game-rules.md` + reconcile  | High     | Relocate ruleset into docs; reconcile to implemented subset (player counts 2/3/4/6, deal counts, sequences-to-win); mark/omit physical-only rules (table-talk, loss-of-card) auto-enforced in digital; add digital-only: tap vs drag, over-five choice (`PendingChoice`), natural-over-jack |
| 2   | API / tRPC reference            | thin           | `app-router.ts`, `game/game.router.ts:40-56`, `history.router.ts:10-14`, `game/routes/*`, `user/auth.ts`                   | New `docs/api-reference.md` (or expand pkg README)| High     | Per-procedure I/O; auth/guest per proc; `version` guard; `RuleViolation`→`BAD_REQUEST`; `onGameEvent` snapshot/cursor; anon limiter; `/api/auth/*` |
| 3   | game-logic library reference    | thin           | `game-logic/src/index.ts` (~40 exports), `types.ts`, `apply-move.ts:1-13`                                                  | New `docs/game-logic-reference.md`              | High     | Export inventory; `applyMove`/`resolveSequenceChoice`/`turnInDeadCard` signatures; `MoveResult` ok/error; `RuleViolation`; `GameEvent`; `Rng` |
| 4   | Data model / DB schema          | no coverage    | `db/schema/games.ts:83-141`, `game-players.ts:27-54`, `game-events.ts:23-41`, `drizzle/0000–0003*.sql`                     | New `docs/data-model.md`                         | Medium   | 3 game tables + auth tables; jsonb shapes; `state-mapping.ts` round-trip; `version`; event-log triple duty; guest vs user; FK behavior |
| 5   | Consolidated env reference      | thin           | `packages/api/src/env.ts` (14 vars), `.env.example`, web `NEXT_PUBLIC_*`                                                   | New `docs/configuration.md` or `development.md` section | Medium | One table: var/scope/required/default/validation/prod; `TRUST_PROXY`; `AUTH_COOKIE_*`; 32-char secret; `DATABASE_URL_TEST`; OAuth gating |
| 6   | Testing strategy                | thin           | `scripts/run-tests.mjs`, `vitest.workspace.ts`, `packages/api/src/test/harness.ts`, `apps/web/e2e/`, `playwright.config.ts`| New `docs/testing.md` or `development.md` section | Medium   | Layers (unit/integration/e2e); integration harness + DB test branch; Playwright 2 projects; `DATABASE_URL_TEST` gating; `run-tests.mjs` |
| 7   | Contributing guide              | no coverage    | `tools/git-hooks/README.md`, `AGENTS.md:37-49`, `.oxlintrc.json`, `.oxfmtrc.json`                                          | New root `CONTRIBUTING.md`                       | Medium   | Commit convention `pNN-tNN`; gate sequence; game-logic purity; oxlint/oxfmt; hooks; do-not-document-as-existing rules |
| 8   | Troubleshooting / failure modes | no coverage    | `env.ts` (fail-fast), `tools/git-hooks/README.md`, `playwright.config.ts` (skip), `packages/api/README.md:79-89`           | New `docs/troubleshooting.md` or `development.md` section | Low | Boot failures; tests not starting servers; cross-site cookie/CORS; stale-version rejection; hooks not running |

## Navigation and Drift

| #   | Surface           | Issue                                              | Evidence              | Disclosure | Link Target        | Severity | Notes                                                       |
| --- | ----------------- | -------------------------------------------------- | --------------------- | ---------- | ------------------ | -------- | ---------------------------------------------------------- |
| 1   | `docs/` (no nav)  | No `index.md` → docs not navigable in-tree          | `find docs`           | inline     | N/A                | High     | Same root cause as High-1                                  |
| 2   | `README.md`       | "More Documentation" omits 3 docs (bruno/hooks/AGENTS)| `README.md:130-137` | inline     | `docs/index.md`    | Low      | Defer to `docs/index.md` once it exists (Low-2)           |
| 3   | `packages/api/README.md` | `health` prose calls public `ping` "authenticated" | `packages/api/README.md:57` | inline | N/A          | Low      | Imprecise overview, not stale enumeration (Medium-6)     |

No nav points to missing files; no files are orphaned from a generated index (there is none).

## Progressive Disclosure Decisions

| Topic                          | Decision  | Keep Inline In                                  | Link Target                          | Evidence                                              |
| ------------------------------ | --------- | ----------------------------------------------- | ------------------------------------ | ----------------------------------------------------- |
| Environment variables          | inline (canonical table) + link_only (others)   | `docs/development.md` / `docs/configuration.md` | the canonical table | `packages/api/src/env.ts`; 5 duplicated doc locations |
| Run/test/migration commands    | link_only | `docs/development.md`                            | `docs/development.md`                 | duplication in package READMEs + root README          |
| Full ruleset detail            | inline    | `docs/game-rules.md`                             | N/A                                  | `game-logic` rules modules                            |
| Per-procedure API contract     | inline    | `docs/api-reference.md`                          | N/A                                  | `game.router.ts`, `history.router.ts`                 |
| `.env.example` GitHub-var parity | ask_user | (config file, not docs)                          | N/A                                  | `.env.example:24-26` vs `env.ts:53-56`                |
| Docs tooling (flat vs app)     | ask_user  | —                                               | —                                    | flat `docs/`; `oat-docs-bootstrap` available          |

## Recommendations

Ordered by leverage. All are backed by evidence above; `oat-docs-apply` must read the cited sources before generating prose.

1. **Add `docs/index.md` with a grouped `## Contents` map** — gives the docs tree a navigable entrypoint (closes High-1).
   - Target: `docs/index.md`
   - Action Type: create
   - Evidence: `find docs` (no index); `README.md:130-137`
   - Confidence: high
   - Disclosure: inline
   - Link Targets: N/A
2. **Relocate + reconcile the ruleset into `docs/game-rules.md`** — an authoritative ruleset already exists in the planning tree; bring it into the published docs and align it with what the MVP implements (closes High-2).
   - Target: `docs/game-rules.md` (relocated via `git mv` from `.oat/repo/reference/planning/2026-rewrite/game-instructions.md`)
   - Action Type: move + update
   - Evidence: source `.oat/repo/reference/planning/2026-rewrite/game-instructions.md:7-98`; implemented behavior `packages/game-logic/src/{types,jack-rules,dead-cards,sequence-detection,win-conditions,board-map,create-game}.ts`
   - Confidence: high
   - Disclosure: inline
   - Link Targets: N/A
3. **Add `docs/api-reference.md`** — enumerated tRPC contract (closes High-3).
   - Target: `docs/api-reference.md`
   - Action Type: create
   - Evidence: `packages/api/src/app-router.ts:12-22`; `game/game.router.ts:40-56`; `history/history.router.ts:10-14`; `game/routes/*`
   - Confidence: high
   - Disclosure: inline
   - Link Targets: N/A
4. **Add `docs/game-logic-reference.md`** — signature-level library reference (closes High-4).
   - Target: `docs/game-logic-reference.md`
   - Action Type: create
   - Evidence: `packages/game-logic/src/index.ts`, `types.ts`, `apply-move.ts:1-13`
   - Confidence: high
   - Disclosure: inline
   - Link Targets: N/A
5. **Add a canonical environment-variable reference and link the other docs to it** — kills env duplication/drift (closes Medium-1, Medium-5).
   - Target: `docs/configuration.md` (or `docs/development.md#environment-reference`)
   - Action Type: create + update (link from README, deployment.md, package READMEs)
   - Evidence: `packages/api/src/env.ts`; `README.md:41-60`; `docs/development.md:16-33`; `docs/deployment.md:37-57`; `apps/web/README.md:37-42`; `packages/api/README.md:16-38`
   - Confidence: high
   - Disclosure: inline + link_only
   - Link Targets: the canonical table
6. **Add `docs/data-model.md`** — DB schema reference (closes Medium-2).
   - Target: `docs/data-model.md`
   - Action Type: create
   - Evidence: `packages/api/src/db/schema/{games,game-players,game-events}.ts`; `state-mapping.ts`; `drizzle/0000–0003*.sql`
   - Confidence: high
   - Disclosure: inline
   - Link Targets: N/A
7. **Add a testing-strategy doc/section** — explains layers + DB test branch (closes Medium-3).
   - Target: `docs/testing.md` (or `docs/development.md` section)
   - Action Type: create
   - Evidence: `scripts/run-tests.mjs`; `packages/api/src/test/harness.ts`; `apps/web/playwright.config.ts`
   - Confidence: medium
   - Disclosure: inline
   - Link Targets: N/A
8. **Add root `CONTRIBUTING.md`** — surfaces commit convention + gates (closes Medium-4).
   - Target: `CONTRIBUTING.md`
   - Action Type: create
   - Evidence: `tools/git-hooks/README.md`; `AGENTS.md:37-49`
   - Confidence: medium
   - Disclosure: inline
   - Link Targets: N/A
9. **Fix `packages/api/README.md` health wording** — `ping` is public (closes Medium-6).
   - Target: `packages/api/README.md:57`
   - Action Type: update
   - Evidence: `packages/api/src/app-router.ts:13-17`
   - Confidence: high
   - Disclosure: inline
   - Link Targets: N/A
10. **Add a Documentation contract block to `AGENTS.md`** — `index.md`/`## Contents`, `.md` links, analyze/apply routing (closes Medium-7, Local Guidance 1-3).
    - Target: `AGENTS.md`
    - Action Type: update
    - Evidence: `AGENTS.md:28-49`
    - Confidence: medium
    - Disclosure: inline
    - Link Targets: N/A
11. **Trim package READMEs to package-specific notes + a link to `docs/`** — reduces duplication (Medium-5).
    - Target: `apps/web/README.md`, `packages/api/README.md`
    - Action Type: update
    - Evidence: overlap refs in Medium-5
    - Confidence: medium
    - Disclosure: link_only
    - Link Targets: `docs/development.md`
12. **Complete or replace README "More Documentation" list** — point at `docs/index.md` (Low-1, Low-2).
    - Target: `README.md:73-81, 130-137`
    - Action Type: update
    - Evidence: `README.md` refs; missing `/`, `/ping`, bruno/hooks/AGENTS links
    - Confidence: high
    - Disclosure: inline
    - Link Targets: `docs/index.md`
13. **(ask_user) Decide docs tooling** — keep flat docs-tree vs. bootstrap an OAT/Fumadocs app (Medium-8); and **(ask_user)** add GitHub OAuth vars to `.env.example` (Low-3, config file outside docs scope).
    - Target: `docs/` tooling decision; `.env.example`
    - Action Type: ask_user
    - Evidence: flat `docs/`; `oat-docs-bootstrap`; `.env.example:24-26` vs `env.ts:53-56`
    - Confidence: medium
    - Disclosure: ask_user
    - Link Targets: N/A

## Apply Contract

- `oat-docs-apply` may only implement recommendations backed by evidence in this artifact.
- All accuracy claims verified (0 contradicted), so no docs-vs-code contradictions must be resolved before applying; the only doc/config inconsistency is the `.env.example` GitHub-var gap (Low-3), gated `ask_user`.
- There are no generated artifacts in this surface; nothing requires regeneration.
- Content-opportunity recommendations (1-8) require `oat-docs-apply` to read the cited `game-logic` / `api` / `db/schema` sources before generating prose; it must not synthesize feature or rules coverage from memory.
- **Bundled source input:** `.oat/repo/reference/planning/2026-rewrite/game-instructions.md` is the canonical Sequence ruleset and is provided to `oat-docs-apply` as the prose base for Recommendation 2. Apply should relocate it into `docs/` via `git mv` (preserving history) and reconcile it to the implemented MVP subset — it must not author the rules from scratch, and must not copy the full physical ruleset verbatim.
- `omit` items (Low-4, Low-5) must stay out of generated docs changes.
- `ask_user` items (Medium-8 docs tooling; Low-3 `.env.example`) require explicit confirmation before action.
- `link_only` items must keep their canonical link targets (env reference; `docs/development.md`).
- If any cited source is missing at apply time, stop and re-run analyze or ask the user rather than inventing a convention.

## Next Step

Run `oat-docs-apply` with this artifact to approve and apply the recommended documentation changes. Recommendations 1-4 (the `docs/index.md` entrypoint and the three High content gaps) are the highest-leverage first batch.
