# Recommendation Pack: `rec-007`

## Target

- Action: create
- Path: `packages/api/CLAUDE.md`
- Provider / Format: claude / CLAUDE.md import shim
- Disclosure: inline

## Evidence

- Depends on `rec-006` (the new `packages/api/AGENTS.md`).
- `.oat/sync/config.json` — `claude.enabled: true`.

## Structural Conventions

- Minimal shim: `@AGENTS.md` (resolves to `packages/api/AGENTS.md`).

## Behavioral Conventions

- Claude Code picks up the scoped API guidance (redaction, version guards, IP rule, routes, migrations) when working under `packages/api/`.

## Counter-Examples

- Duplicating `packages/api/AGENTS.md` content.
- Importing the root `AGENTS.md` explicitly instead of the directory-local one.

## New-File Workflow

1. Create `packages/api/CLAUDE.md` in the same pass as `rec-006`.
2. Content: `@AGENTS.md`.

## Preferred Default for New Files

N/A.

## Claim Corrections

- none.

## Generation Constraints

- Must Include: `@AGENTS.md`.
- Must Not Include: duplication.
- Link Targets: `packages/api/AGENTS.md`.
