# Recommendation Pack: `rec-002`

## Target

- Action: create
- Path: `CLAUDE.md`
- Provider / Format: claude / CLAUDE.md import shim
- Disclosure: inline

## Evidence

- `.oat/sync/config.json` — `claude.enabled: true` (provider active).
- `AGENTS.md:1` — canonical instructions exist at root.
- `.oat/sync/manifest.json` — only `contentType: skill|agent` entries; `oat sync` does **not** generate `CLAUDE.md`, so it must be authored.
- Repo-wide `find` — no `CLAUDE.md` exists anywhere today.

## Structural Conventions

- A minimal Claude memory shim: the `@AGENTS.md` import at the top, then optional Claude-only notes.

## Behavioral Conventions

- Claude Code loads `CLAUDE.md` as project memory and resolves `@AGENTS.md` to import the canonical instructions, keeping a single source of truth.

## Counter-Examples

- Copying any `AGENTS.md` content into `CLAUDE.md` (creates drift).
- A `CLAUDE.md` that omits the import (Claude then never sees `AGENTS.md`).

## New-File Workflow

1. Create `CLAUDE.md` at repo root.
2. First line content: `@AGENTS.md`.
3. Add Claude-specific notes only if genuinely needed (none required today).

## Preferred Default for New Files

N/A.

## Claim Corrections

- none.

## Generation Constraints

- Must Include: `@AGENTS.md`.
- Must Not Include: duplication of `AGENTS.md`.
- Link Targets: `AGENTS.md`.
