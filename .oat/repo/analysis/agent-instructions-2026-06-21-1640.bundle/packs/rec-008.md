# Recommendation Pack: `rec-008`

## Target

- Action: create
- Path: `packages/game-logic/AGENTS.md`
- Provider / Format: agents_md / AGENTS.md
- Disclosure: link_only-heavy (thin delta)

## Evidence

- `packages/game-logic/README.md:46-52` — "Design Rules": deterministic by default, thread `Rng`, return typed rule violations instead of throwing, no framework/db/network/DOM imports, focused tests per change.
- `packages/game-logic/README.md:22-38` — public surface exported from `src/index.ts`.
- `packages/game-logic/package.json` — only runtime dep is `zod` (framework-free confirmed).
- `AGENTS.md:43` — root already states the framework-free rule (do not repeat verbatim; link/extend).

## Structural Conventions

- Pure functions over immutable domain state; co-located `*.test.ts` per module; public APIs exported through `src/index.ts` (no deep cross-package imports).
- Relative imports carry explicit `.ts` extensions; type-only imports use `import type`.

## Behavioral Conventions

- Keep functions deterministic by default; thread an injected `Rng` for non-deterministic operations so tests can control them.
- Return typed `RuleViolation`s for expected illegal moves instead of throwing.
- No React/Next/Fastify/database/network/DOM imports (already a root non-negotiable — reinforce briefly, link the README).

## Counter-Examples

- Throwing on an expected illegal move instead of returning a typed violation.
- Calling `Math.random()` directly instead of the threaded `Rng` (breaks determinism/tests).
- Importing from a sibling package's deep path instead of its public `index.ts`, or adding a framework/db import here.

## New-File Workflow

1. New rule module → pure function(s) + co-located `*.test.ts`.
2. Export new public APIs through `src/index.ts`.

## Preferred Default for New Files

- New rules module → pure function + co-located `*.test.ts`; export via `src/index.ts`.

## Claim Corrections

- none.

## Generation Constraints

- Must Include: framework-free rule (brief, linking README); typed rule violations; `Rng` threading; `index.ts` public surface.
- Must Not Include: a full restatement of the README; root command restatement. Keep the file very thin (~20–40 lines).
- Link Targets: `packages/game-logic/README.md`.
