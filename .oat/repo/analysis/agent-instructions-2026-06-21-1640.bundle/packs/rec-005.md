# Recommendation Pack: `rec-005`

## Target

- Action: create
- Path: `apps/web/CLAUDE.md`
- Provider / Format: claude / CLAUDE.md import shim
- Disclosure: inline

## Evidence

- Depends on `rec-004` (the new `apps/web/AGENTS.md`).
- `.oat/sync/config.json` — `claude.enabled: true`.

## Structural Conventions

- Minimal shim: `@AGENTS.md` (resolves to `apps/web/AGENTS.md`).

## Behavioral Conventions

- Claude Code picks up the scoped web guidance when working under `apps/web/`.

## Counter-Examples

- Duplicating `apps/web/AGENTS.md` content.
- Importing the root `AGENTS.md` explicitly instead of the directory-local one.

## New-File Workflow

1. Create `apps/web/CLAUDE.md` in the same pass as `rec-004`.
2. Content: `@AGENTS.md`.

## Preferred Default for New Files

N/A.

## Claim Corrections

- none.

## Generation Constraints

- Must Include: `@AGENTS.md`.
- Must Not Include: duplication.
- Link Targets: `apps/web/AGENTS.md`.
