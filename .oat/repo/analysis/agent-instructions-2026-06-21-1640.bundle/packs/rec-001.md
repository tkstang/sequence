# Recommendation Pack: `rec-001`

## Target

- Action: update
- Path: `AGENTS.md`
- Provider / Format: agents_md / AGENTS.md
- Disclosure: inline

## Evidence

- `AGENTS.md:1-26` — OAT "Tool Packs" block occupies the first screenful, pushing project snapshot/commands/non-negotiables down (salience issue).
- `AGENTS.md:28-49` — existing "Sequence Project Guidance" (accurate, keep) but prose commands, no snapshot, no References.
- `tsconfig.base.json:10-11` — `allowImportingTsExtensions` + `verbatimModuleSyntax` underpin the import convention.
- `package.json:scripts` — canonical gates (`typecheck`, `lint`, `format:check`, `test`, `build`).
- `docs/architecture.md`, `docs/development.md`, `docs/deployment.md`, package READMEs — link targets for a References section.

## Structural Conventions

- Keep the tool-managed `<!-- OAT tools -->` … `<!-- END OAT tools -->` block intact and in place.
- Add a 2–4 line **project snapshot** (pnpm workspace: `apps/web` Next.js client, `packages/api` Fastify/tRPC/Drizzle, `packages/game-logic` framework-free engine; Node 24, pnpm 10.17+).
- Add a fenced **canonical commands** block (`pnpm install`; gates `pnpm typecheck && pnpm lint && pnpm format:check && pnpm test && pnpm build`; `pnpm --filter @sequence/web e2e` when `DATABASE_URL_TEST` is set).
- Add a short **Code Style** section (≤3 lines).
- Add a **References** section linking the 4 docs pages + 4 READMEs.

## Behavioral Conventions

- Relative imports use explicit `.ts`/`.tsx` extensions; type-only imports use `import type` (`verbatimModuleSyntax`).
- Treat gates as the Definition of Done for code changes.

## Counter-Examples

- Restating oxfmt/oxlint trivia (quote style, semicolons, print width, import order) — these are tool-enforced; reference `pnpm format` / `pnpm lint` instead.
- Duplicating README/architecture prose inline — link instead.
- Removing or reordering the OAT-managed block.

## New-File Workflow

1. Edit `AGENTS.md` in place (do not recreate); insert snapshot/commands/Code Style/References around the existing project section.
2. Leave the OAT block untouched.

## Preferred Default for New Files

N/A (this is an update to an existing file).

## Claim Corrections

- none — all existing `AGENTS.md` claims were verified accurate.

## Generation Constraints

- Must Include: project snapshot; canonical command block; import convention (`.ts`/`.tsx` extensions + `import type`); References to `docs/architecture.md`, `docs/development.md`, `docs/deployment.md`, `README.md`, and the three package READMEs.
- Must Not Include: oxfmt/oxlint-enforced style prose; duplicated doc content; changes to the OAT block.
- Link Targets: `docs/architecture.md`, `docs/development.md`, `docs/deployment.md`, `apps/web/README.md`, `packages/api/README.md`, `packages/game-logic/README.md`.
