export default function HomePage() {
  return (
    <main>
      <section>
        <h1>Sequence Rewrite Kickoff</h1>
        <p>
          The new App Router surface is ready. Migrate legacy pages incrementally and wire up Panda CSS,
          Firebase, and the authoritative game engine here.
        </p>
      </section>
    </main>
  );
}
