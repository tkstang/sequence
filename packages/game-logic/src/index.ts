/**
 * @sequence/game-logic — the pure rules engine.
 *
 * Placeholder export until Phase 2 populates the public surface
 * (types, reducer, board map, deck, sequence detection, …).
 */
export const PACKAGE_NAME = '@sequence/game-logic';
