/**
 * @sequence/api — Fastify 5 + tRPC 11 host for the Sequence game.
 *
 * Placeholder export until Phase 3 introduces the server, tRPC router,
 * Drizzle schema, and Better Auth integration.
 */
export const PACKAGE_NAME = '@sequence/api';
